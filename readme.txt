Traffic demand is extracted on two stages:

-call Prerequisites with command line arguments congiguration file and routing interval (python3 Prerequisites.py wcfg 50)
-call One of the Car following momel algorithms whith command line arguments configuration file, routing interval and the corresponding parameters (KraussOrig outingAlgorithm minGap tau)
